# Add custom window config in this file.

# Tag Chromium Browser
windowrule = tag +chromium-based-browser, match:class chromium-browser

# Android emulator: tiled like any window (no rule). The device window fills its
# tile and the emulator letterboxes the phone aspect; its toolbar floats on its own.
