# Add custom window config in this file.

# Tag Chromium Browser
windowrule = tag +chromium-based-browser, match:class chromium-browser

# Float the Android emulator — tiling forces its fixed device size and hides the
# boot screen. Both the device window and its toolbar share class "Emulator".
windowrule = float on, match:class ^(Emulator)$
